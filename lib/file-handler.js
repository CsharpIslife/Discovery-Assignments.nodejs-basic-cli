const fs = require('fs');  /* use the fs library to write text -> file */ 

module.exports = {
    emptyMethod:() => {
        console.log('remove me');
    },

    /* TODO: write a method that writes a string to file. (encoding = utf8) */ 
    // writeFile: (..) => {
 
    // }
}

 /* TODO: (Hint) do some sort of basic validation to check if the given file path is valid */
const validateInput = () => {

}