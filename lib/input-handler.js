const inquirer = require('inquirer');

 /* TODO: write a method that returns the user prompt and expose it in module.exports */
module.exports = {
    
}

// (Hint) define the user prompt (questions) in this array
const prompt = [];